[package]
name = "periodical_brief"
version = "0.1.0"
edition = "2021"
description = "bitbank periodical-brief — reference implementation (proposal)"
license = "MIT"

[dependencies]
serde_json = "1"
chrono = "0.4"
ureq = "2"

[profile.release]
lto = true
